/*
*
* {{properCase name }} reducers
* make by huynq
*/
import { Reducer } from 'redux';
// eslint-disable-next-line @typescript-eslint/no-unused-vars
import * as types from './constants';
import { Actions{{properCase name }}, Store{{properCase name }} } from "./types";

const initState : Store{{properCase name }} = { };

const reducers{{properCase name }} : Reducer<Store{{properCase name }}, Actions{{properCase name }}> = (state = initState, actions: Actions{{properCase name }}) => {
switch (actions.type) {
default:
return { ...state };
}
};

export default reducers{{properCase name }};